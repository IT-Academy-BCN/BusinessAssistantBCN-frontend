import {BigMallsPageComponent} from "./big-malls-page.component";

describe('BigMallsPageComponent', () => {
  let component: BigMallsPageComponent;

  beforeEach(() => {

  });

  it( 'When Search is clicked, must redirect correctly with the neccesary parameters', () => {
  });

});
